# Example environment variables for local development
# Copy to a file named .env in the same folder and edit values for your environment.

# Server port
PORT=3000

# JWT secret used to sign authentication tokens. Replace with a long random secret in production.
JWT_SECRET=replace_this_with_a_strong_secret

# DynamoDB local (if using Data/docker-compose)
AWS_REGION=us-east-1
AWS_ENDPOINT=http://localhost:8000
# Development helper: use an in-memory store instead of DynamoDB (useful for smoke tests)
USE_IN_MEMORY_DB=true